# Write Jest unit tests for HabitFlow's habitStorage

**Chapter 17 — Testing HabitFlow**  
Used in: Step 2

---

```
Write Jest unit tests for HabitFlow's habitStorage.ts covering:

- Creating a habit with valid data returns an object with an ID

- Creating a habit without a name throws an error

- toggleCompletion toggles on and off correctly

- getStreak returns 0 for no completions

- getStreak returns correct count for consecutive days

- getStreak handles broken streaks correctly

- deleteHabit removes the habit Mock AsyncStorage. Show me how to run the tests.
```
