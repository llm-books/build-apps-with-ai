# Set up a Claude Code hook that automatically runs npx jest…

**Chapter 17 — Testing HabitFlow**  
Used in: Step 4

---

```
Set up a Claude Code hook that automatically runs npx jest whenever any file in src/services/ is modified. This way, if a code change breaks the storage logic, the tests catch it immediately. Show me how to configure this in the .claude/ directory.
```
