# Update CLAUDE

**Chapter 12 — User Accounts And Cloud Sync**  
Used in: Step 8

---

```
Update CLAUDE.md: Firebase integrated. Authentication working with email/password (sign up, login, logout, password reset). Data now syncs to Firestore with AsyncStorage as offline cache. Security rules restrict users to their own data. Auth flow gates the app (logged out = welcome/login, logged in = main app).
```
