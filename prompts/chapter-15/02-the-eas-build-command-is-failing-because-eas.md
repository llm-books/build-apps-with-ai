# The eas build command is failing because eas

**Chapter 15 — From Expo Go To A Real App**  
Used in: Step 4

---

```
The eas build command is failing because eas.json has a submit section with empty values (serviceAccountKeyPath, ascAppId, appleTeamId). Remove the entire submit section from eas.json for now. We will add it back when we are ready to publish.
```
