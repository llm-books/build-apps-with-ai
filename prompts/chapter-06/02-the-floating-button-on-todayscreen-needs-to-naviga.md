# The floating + button on TodayScreen needs to navigate to…

**Chapter 6 — The Add Habit Screen**  
Used in: Step 3

---

```
The floating + button on TodayScreen needs to navigate to AddHabitScreen when tapped. Wire up the navigation using navigation.navigate('AddHabit\textquotesingle).
```
