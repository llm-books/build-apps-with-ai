# The {[}screen name{]} screen looks correct on my phone but…

**Chapter 23 — Troubleshooting Common Issues**

---

```
The {[}screen name{]} screen looks correct on my phone but the layout is broken on {[}describe the other device{]}. Specifically, {[}describe what looks wrong: elements overlapping, text cut off, too much empty space, etc.{]}. Make sure the layout uses flexible sizing instead of fixed pixel values.
```
