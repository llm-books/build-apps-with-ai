# Set up RevenueCat in HabitFlow for subscriptions, using the…

**Chapter 21 — Making Money From Your App**  
Used in: Step 1

---

```
Set up RevenueCat in HabitFlow for subscriptions, using the react-native-purchases package. Create a PurchasesService that initializes RevenueCat with my API key, fetches the available offerings, and exposes a simple isPlus() helper that returns whether the current user has an active "Plus" subscription. Explain exactly where I paste my RevenueCat API key and warn me not to commit it to Git.
```
